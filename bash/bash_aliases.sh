#!/bin/sh
#
# ~/.bash_aliases
#
# A subset of the shell configuration for setting aliases.

# Set aliases
alias cls="clear"
alias ll="ls -alh"
alias lld="ls -adlh"
alias llr="ls -lRath"
alias more="\${PAGER:-more}"
